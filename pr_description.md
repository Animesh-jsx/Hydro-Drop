🎯 **What:** Addressed testing gap by adding tests for `InquiryForm` and fixing the form validation logic.
📊 **Coverage:** Tests verify form elements render, `required` validation logic is verified using RTL, form submission correctly uses `event.preventDefault()` without reloading.
✨ **Result:** Enhanced the test coverage for the main inquiry form, increased reliability by preventing form reloads and adding `required` validations, along with standard `name` attributes mapped with `htmlFor` to corresponding labels.
